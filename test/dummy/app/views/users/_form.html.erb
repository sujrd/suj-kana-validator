<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Kaji"%><br />
    <%= f.text_field :name_kana %>
  </div>

  <div class="field">
    <%= f.label "Katakana" %><br />
    <%= f.text_field :name_halfkana %>
  </div>

  <div class="field">
    <%= f.label "Hiragana" %><br />
    <%= f.text_field :name_hiragana %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
